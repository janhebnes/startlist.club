@model FlightJournal.Web.Models.Club
@if (Request.IsAuthenticated)
{
    <p>Aktive flyveklub filtrering sat til </p>
    @Html.DisplayFor(modelItem => Model.ShortName)
    @Html.DisplayFor(modelItem => Model.Name)
}